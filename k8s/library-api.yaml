# Library API - Stable Deployment (메인 백엔드)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: library-api-stable
  namespace: history-app
  labels:
    app: library-api
    version: stable
spec:
  replicas: 2
  selector:
    matchLabels:
      app: library-api
      version: stable
  template:
    metadata:
      labels:
        app: library-api
        version: stable
    spec:
      containers:
      - name: library-api
        image: 355627705292.dkr.ecr.ap-northeast-2.amazonaws.com/library-api:stable
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: app-config
        - secretRef:
            name: app-secrets
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 15
          periodSeconds: 10
---
# Library API - Canary Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: library-api-canary
  namespace: history-app
  labels:
    app: library-api
    version: canary
spec:
  replicas: 1
  selector:
    matchLabels:
      app: library-api
      version: canary
  template:
    metadata:
      labels:
        app: library-api
        version: canary
    spec:
      containers:
      - name: library-api
        image: 355627705292.dkr.ecr.ap-northeast-2.amazonaws.com/library-api:canary
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: app-config
        - secretRef:
            name: app-secrets
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
---
# Library API Service
apiVersion: v1
kind: Service
metadata:
  name: library-api
  namespace: history-app
spec:
  selector:
    app: library-api
  ports:
  - port: 8000
    targetPort: 8000
  type: ClusterIP
