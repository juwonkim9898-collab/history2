# Auth API - Stable Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-api-stable
  namespace: history-app
  labels:
    app: auth-api
    version: stable
spec:
  replicas: 2
  selector:
    matchLabels:
      app: auth-api
      version: stable
  template:
    metadata:
      labels:
        app: auth-api
        version: stable
    spec:
      containers:
      - name: auth-api
        image: 355627705292.dkr.ecr.ap-northeast-2.amazonaws.com/auth-api:stable
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: app-config
        - secretRef:
            name: app-secrets
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 15
          periodSeconds: 10
---
# Auth API - Canary Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-api-canary
  namespace: history-app
  labels:
    app: auth-api
    version: canary
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-api
      version: canary
  template:
    metadata:
      labels:
        app: auth-api
        version: canary
    spec:
      containers:
      - name: auth-api
        image: 355627705292.dkr.ecr.ap-northeast-2.amazonaws.com/auth-api:canary
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: app-config
        - secretRef:
            name: app-secrets
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
---
# Auth API Service
apiVersion: v1
kind: Service
metadata:
  name: auth-api
  namespace: history-app
spec:
  selector:
    app: auth-api
  ports:
  - port: 8000
    targetPort: 8000
  type: ClusterIP
